<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <service
            android:name=".service.CoreService"
            android:enabled="true"
            android:exported="true"></service>
        <service
            android:name=".DbIntentService"
            android:exported="false" />

        <activity
            android:name=".activity.CourseAddActivity"
            android:label="@string/course_add" />
        <activity
            android:name=".activity.NotificationActivity"
            android:label="@string/title_activity_notifications" />
        <activity
            android:name=".activity.UserProfileActivity"
            android:label="@string/title_activity_user_profile" />
        <activity android:name=".activity.CourseInformationActivity" />
        <activity android:name=".activity.CourseNoticeActivity" />
        <activity android:name=".activity.CourseFileActivity" />
        <activity android:name=".activity.CourseMenuActivity" />
        <activity android:name=".activity.CourseChatActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>